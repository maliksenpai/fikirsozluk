<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".UI.Activities.ReadPostActivity">

    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|right|end"
            android:layout_marginBottom="20dp"
            android:layout_marginRight="20dp"
            android:clickable="true" app:srcCompat="@android:drawable/ic_menu_add"
            android:id="@+id/postnewpost"/>
    <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:id="@+id/readpostlist"
            android:layout_height="366dp" tools:listitem="@layout/postreadlist">
    </androidx.recyclerview.widget.RecyclerView>
    <EditText
            android:layout_width="329dp"
            android:layout_height="141dp"
            android:inputType="textMultiLine"
            android:textSize="10dp"
            android:textColor="#000"
            android:layout_gravity="bottom"
            android:ems="10"
            android:id="@+id/readnewposttext"
    android:visibility="invisible"/>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|right|end"
            android:layout_marginBottom="20dp"
            android:layout_marginRight="20dp"
            android:visibility="invisible"
            android:clickable="true" app:srcCompat="@android:drawable/ic_menu_add"
            android:id="@+id/readnewpostbutton"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>